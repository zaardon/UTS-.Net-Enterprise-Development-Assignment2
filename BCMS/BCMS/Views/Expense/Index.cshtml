@model IEnumerable<BlueConsultingManagementSystem.Models.Expense>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExpensePK }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExpensePK }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExpensePK })
        </td>
    </tr>
}

</table>
